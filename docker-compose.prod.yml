version: '3.8'
services:
  api:
    build: .
    container_name: trueroas_api
    restart: always
    ports:
      - "8001:8001"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    logging:
      driver: "json-file"